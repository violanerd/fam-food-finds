      <form class="m-0 flex flex-col items-center justify-center px-4 py-4 md:px-12 mt-20" autocomplete="off">
              <div class="flex">
                <div class="flex items-center justify-center">
                  <div class="flex border-2 rounded mb-0">
                    <input type="text" class="px-4 py-2 w-80" placeholder="Search by restaurant name..." id="restaurant_name" />
                    <button class="flex items-center justify-center px-4 border-l" id="btn-search">
                      <svg
                        class="hover:text-darkGrayishBlue w-6 h-6 text-gray-600"
                        fill="currentColor"
                        xmlns="http://www.w3.org/2000/svg"
                        viewBox="0 0 24 24"
                      >
                        <path
                          d="M16.32 14.9l5.39 5.4a1 1 0 0 1-1.42 1.4l-5.38-5.38a8 8 0 1 1 1.41-1.41zM10 16a6 6 0 1 0 0-12 6 6 0 0 0 0 12z"
                        ></path>
                      </svg>
                    </button>
                  </div>
                </div>
              </div>
      </form>
{{!-- list items for search box --}}
<div class="listItemSearch hidden" id="list-items">
  <ul id="menuItemsSearch">
    {{#each restaurants as |restaurant|}}
      {{> listItems restaurant}}
    {{/each}}
  </ul>
</div>

{{!-- restaurant items --}}
  <div class="" id="restaurant-container">
    <div class="flex-1 max-w-4xl mx-auto p-10">

      <div class=" flex items-end justify-end gap-4">
        <p>Search by:</p>
                <button class="relative flex justify-start items-center focus:outline-none shadow text-gray-600 border rounded focus:ring ring-gray-200 group">
                  <p class="px-4">Categories</p>
                  <span class="border-l hover:bg-gray-100">
                    <i class='fas fa-angle-down hover:text-darkGrayishBlue w-6 h-6 text-gray-600'></i>
                  </span>
                  <div class="absolute hidden group-focus:block top-full min-w-full w-max bg-white shadow-md mt-1 rounded">
                    <ul class="text-left border rounded" id="category">
                      {{#each categories as |category|}}
                         <a href="/"><li class="px-4 py-1 hover:bg-red-50 border-b"> {{category.category_name}}</li></a>
                      {{/each}}
                    </ul>
                  </div>
                </button>
      </div>
      <div class="" id="restaurant-container">
        <ul class="grid grid-cols-1 gap-4 md:grid-cols-3 md:gap-8 mt-3">  
        {{#each restaurants as |restaurant|}}
          <li class="">{{> restaurantItem restaurant}}</li>
        {{/each}}
        </ul>
      </div>
    </div>
  </div>
<script src="/javascript/search-restaurant.js"></script>
<div class="border shadow-sm ml-20 mr-20 mb-3">
  <div class="text-2xl p-3 mt-0 bg-darkGrayishBlue hover:text-cyan-50">
    <a href="/restaurant/{{id}}">
      {{restaurant_name}}
    </a>
  </div>
  <ul class="flex flex-col md:flex-row md:gap-4 text-right m-5">
        <li class=" flex-grow text-left">
            <a href="{{restaurant_url}}">
            {{restaurant_url}}
            </a>
            <div class="">
                {{createdAt}}
            </div>
        </li>
        <li class="m-3">
            Rating
        </li>
   </ul>
  <ul class="flex flex-col md:flex-row md:gap-4 text-right m-5 justify-end">
    <li href="/restaurant/{{id}}" class="border p-3 bg-darkGrayishBlue hover:bg-slate-300 hover:text-cyan-50 text-center">
      View
    </li>
    <li href="dashboard/edit/{{id}}" class="border p-3 bg-darkGrayishBlue hover:bg-slate-300 hover:text-cyan-50 text-center">
      Edit
    </li>
    <li href="dashboard/edit/{{id}}" class="border p-3 bg-darkGrayishBlue hover:bg-slate-300 hover:text-cyan-50 text-center">
      Delete
    </li>
  </ul>
</div>